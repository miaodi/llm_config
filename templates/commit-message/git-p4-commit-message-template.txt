# Shared personal Git/P4 message format. Remove comments and unused placeholders.
# Scope is optional. Keep the summary concrete and preferably under 72 characters.
type(scope): describe the change

[Optional body: resulting behavior, reason, and material tradeoffs]

[Optional validation: checks actually performed and outcomes, or a relevant limitation]

[Optional footer: supplied issue/review references or breaking-change details]

[For P4 descriptions only: STARs: <actual ID>]

# Suggested types: feat, fix, refactor, docs, style, test, chore, perf, build, ci.
# Do not invent validation or identifiers. Omit empty optional sections.
# The personal P4 convention requires the actual STARs ID as the final line.
# Git messages do not require STARs metadata unless the project requests it.
